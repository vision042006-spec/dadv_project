[build]
  base    = "frontend"
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback – all routes served from index.html
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
